version: "3"

services:
  web_app:
    image: letavra/lab2:v1
    container_name: web_app
    ports:
      - "5000:5000"
    restart: always
    volumes:
      - data:/app/data
    build: ../../../../app_python
volumes:
  data: {}